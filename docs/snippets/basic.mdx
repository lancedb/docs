{/* Auto-generated by scripts/mdx_snippets_gen.py. Do not edit manually. */}

export const TsAddColumns = "await tbl.addColumns([\n  { name: \"double_price\", valueSql: \"cast((price * 2) as Float)\" },\n]);\n";

export const TsAddData = "const data = [\n  { vector: [1.3, 1.4], item: \"fizz\", price: 100.0 },\n  { vector: [9.5, 56.2], item: \"buzz\", price: 200.0 },\n];\nawait tbl.add(data);\n";

export const TsAlterColumns = "await tbl.alterColumns([\n  {\n    path: \"double_price\",\n    rename: \"dbl_price\",\n    dataType: \"float\",\n    nullable: true,\n  },\n]);\n";

export const TsAlterColumnsVector = "await tbl.alterColumns([\n  {\n    path: \"vector\",\n    dataType: new arrow.FixedSizeList(\n      2,\n      new arrow.Field(\"item\", new arrow.Float16(), false),\n    ),\n  },\n]);\n";

export const TsConnect = "const db = await lancedb.connect(databaseDir);\n";

export const TsCreateEmptyTable = "\nconst schema = new arrow.Schema([\n  new arrow.Field(\"id\", new arrow.Int32()),\n  new arrow.Field(\"name\", new arrow.Utf8()),\n]);\n\nconst emptyTbl = await db.createEmptyTable(\"empty_table\", schema);\n";

export const TsCreateF16Table = "const db = await lancedb.connect(databaseDir);\nconst dim = 16;\nconst total = 10;\nconst f16Schema = new Schema([\n  new Field(\"id\", new Int32()),\n  new Field(\n    \"vector\",\n    new FixedSizeList(dim, new Field(\"item\", new Float16(), true)),\n    false,\n  ),\n]);\nconst data = lancedb.makeArrowTable(\n  Array.from(Array(total), (_, i) => ({\n    id: i,\n    vector: Array.from(Array(dim), Math.random),\n  })),\n  { schema: f16Schema },\n);\nconst _table = await db.createTable(\"f16_tbl\", data);\n";

export const TsCreateIndex = "await tbl.createIndex(\"vector\");\n";

export const TsCreateTable = "const _tbl = await db.createTable(\n  \"myTable\",\n  [\n    { vector: [3.1, 4.1], item: \"foo\", price: 10.0 },\n    { vector: [5.9, 26.5], item: \"bar\", price: 20.0 },\n  ],\n  { mode: \"overwrite\" },\n);\n";

export const TsCreateTableExistsOk = "const tbl = await db.createTable(\"myTable\", data, {\n  existOk: true,\n});\n";

export const TsCreateTableOverwrite = "const tbl = await db.createTable(\"myTable\", data, {\n  mode: \"overwrite\",\n});\n";

export const TsCreateTableWithSchema = "const schema = new arrow.Schema([\n  new arrow.Field(\n    \"vector\",\n    new arrow.FixedSizeList(\n      2,\n      new arrow.Field(\"item\", new arrow.Float32(), true),\n    ),\n  ),\n  new arrow.Field(\"item\", new arrow.Utf8(), true),\n  new arrow.Field(\"price\", new arrow.Float32(), true),\n]);\nconst data = [\n  { vector: [3.1, 4.1], item: \"foo\", price: 10.0 },\n  { vector: [5.9, 26.5], item: \"bar\", price: 20.0 },\n];\nconst tbl = await db.createTable(\"myTable\", data, {\n  schema,\n});\n";

export const TsDeleteRows = "await tbl.delete('item = \"fizz\"');\n";

export const TsDropColumns = "await tbl.dropColumns([\"dbl_price\"]);\n";

export const TsDropTable = "await db.dropTable(\"myTable\");\n";

export const TsImports = "import * as lancedb from \"@lancedb/lancedb\";\nimport * as arrow from \"apache-arrow\";\n";

export const TsImportsArrow = "import {\n  Field,\n  FixedSizeList,\n  Float16,\n  Int32,\n  Schema,\n  Utf8,\n} from \"apache-arrow\";\n";

export const TsOpenTable = "const _tbl = await db.openTable(\"myTable\");\n";

export const TsTableNames = "const tableNames = await db.tableNames();\n";

export const TsVectorSearch = "const res = await tbl.search([100, 100]).limit(2).toArray();\n";

